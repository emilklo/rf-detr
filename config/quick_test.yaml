# Quick test configuration - 1 epoch with smaller batch

dataset:
  dir: "datasets/lars_rfdetr"

training:
  epochs: 1
  batch_size: 4
  grad_accum_steps: 4
  lr: 1.0e-4

output:
  dir: "auto"  # auto = generate based on training params

hardware:
  gpu: "cuda"

model:
  pretrain_weights: null
